# =============================================================================
# debix.conf  —  target board configuration
# Edit TARGET_IP before starting the container.
# =============================================================================

TARGET_IP=192.168.1.xxx   # IP address of your Debix EVK board
TARGET_USER=root          # SSH user on the board (default: root)
